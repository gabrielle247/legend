//TODO Powersync is super broken that errors will go unnoticed
//TODO Separate powersync and the powersyn usage example a database created by powersync and powersync initilization
//TODO Scan the project for placebo
//TODO Add functionality to most buttons
//TODO Separate the powersync and user auth but closely tie them to auth - powersync; 
//TODO The way data is being handled is bound to make the app crash
//TODO View models are placebos and do not have much when it comes to real data they need to be wired

import 'package:flutter/material.dart';

class Major extends StatelessWidget {
  const Major({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: 
      Text("This file is not ui: ------- 1. It contains project requirements todos"),
    );
  }
}